import * as palette from './palette';
import { spacing } from './spacing';

export const headerBackgroundColor = palette.cedar;
export const pageBackgroundColor = palette.cocoaBrown;
export const smallScreenMargin = spacing.sm;
