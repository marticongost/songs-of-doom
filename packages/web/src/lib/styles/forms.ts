import * as palette from './palette';

export const controlBackgroundColor = palette.shade;
export const controlBorderColor = palette.ash;
export const controlBorder = `2px solid ${controlBorderColor}`;
export const controlHeight = '2.6em';
